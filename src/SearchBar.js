import React from 'react';

const SearchBar = props => {
    return(
        <div>
            Search for a product:
            <input type="text"/>
        </div>
    );
}

export default SearchBar;